spring:
  application:
    name: billing-service

  datasource:
    url: jdbc:postgresql://localhost:5433/db_billing_users
    username: billing
    password: qwerty123
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true

  rabbitmq:
    host: localhost
    port: 5672
    username: admin
    password: admin
    listener:
      simple:
        retry:
          enabled: true
          initial-interval: 3000
          max-attempts: 3

  # 🛡️ ESTA ES LA PROPIEDAD QUE CORRIGE TU ERROR
  amqp:
    deserialization:
      trust:
        all: true

server:
  port: 8082

# Configuración personalizada de RabbitMQ
rabbitmq:
  exchange:
    pedidos: pedidos.exchange
    tracking: tracking.exchange
  queue:
    pedido-creado: pedido.creado
    pedido-estado: pedido.estado.actualizado
    repartidor-ubicacion: billing.repartidor.ubicacion.actualizada
  routing-key:
    pedido-creado: pedido.creado
    pedido-estado: pedido.estado.actualizado
    repartidor-ubicacion: repartidor.ubicacion.actualizada

jwt:
  secret: MiSuperClaveSecretaMuyLarga123456789
  issuer: auth-service